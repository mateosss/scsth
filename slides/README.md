# SLAM Thesis Slides

Using [Slidev](https://github.com/slidevjs/slidev).

To start the slide show:

- `npm install`
- `npm run dev`
- visit <http://localhost:3030>

To export it as a pdf then run:

- `npm install --only=dev`
- `npm run export # optional flags: --dark, --with-clicks`
